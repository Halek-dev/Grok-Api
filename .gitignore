# Never commit the key or the spend record.
# .env.* covers backups and per-machine variants (.env.local, .env.bak, ...),
# which is how a real key nearly got staged once.
.env
.env.*
!.env.example
usage.jsonl
images/
favourites.json

node_modules/
npm-debug.log*
.DS_Store
Thumbs.db
